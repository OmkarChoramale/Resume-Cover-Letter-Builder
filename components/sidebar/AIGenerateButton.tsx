import type { FC } from 'react';

// This component is intentionally left empty to remove the AI generation feature from the UI as requested.
// It returns null to ensure it doesn't render anything, wherever it might be used.
const AIGenerateButton: FC = () => {
  return null;
};

export default AIGenerateButton;
